# Калибровка правил `2026-09-21.1` шумом (T16)

Дата: 2026-09-21. Эталонная стратегия для структурного соответствия шума — `trend-tsmom-crypto` (`trend.yaml`). Каждый прогон ниже — настоящий вызов `evaluate_spec`: те же правила, косты, accrual и универс, что и для любой реальной стратегии; каждый прогон записан в журнал испытаний (`trial`).

**Вопрос:** если в отбор подать заведомо беспредметный шум — стратегии без какого-либо сигнала, но с сопоставимой гросс-экспозицией, оборотом и числом позиций, — как часто отбор говорит «да», и какой Шарп шум достигает в лучшем случае из N попыток?

## Ревизия 2026-09-21: генератор `bootstrap_time` содержал look-ahead

Первая версия этого отчёта (тот же снапшот, те же 200+200 попыток) сообщала:

| серия | доля допуска | лучший sharpe_net |
|---|---:|---:|
| dollar-neutral | 3.0% | 1.847 |
| без ограничения на нетто | 13.0% | 1.684 |

и вывод «серия без ограничения на нетто допускается заметно чаще — планка
ловит бету и carry».

**Обе цифры были искажены.** `bootstrap_time` (`qlab.calibration.noise`)
сэмплировал строку-источник `j` равномерно по ВСЕМУ ряду для каждого `t`,
поэтому решению на день `t` мог достаться вектор весов, посчитанный
трендовой эталонной стратегией по ценам ПОСЛЕ `t` — например, по 90-дневному
лукбэку, захватывающему `t+1..t+300`. Для моментум-сигнала это прямая
утечка: книга «знала» на день `t`, куда пойдёт рынок после `t`, и именно
поэтому `bootstrap_time` оказался лучшим генератором в обеих сериях (лучший
Шарп 1.847/1.684 против ≤0.94 у трёх остальных, причинных генераторов —
разбивка по генераторам ниже). Проверка на бесплатный проезд по костам
(оборот 200–247 годовых против 36 у trend) была верной, но она ловит только
нулевой оборот, а не утечку по времени — отсюда и разные проверки нужны
для разных дефектов.

Заявленный разрыв 13.0% против 3.0% почти целиком держался на
`bootstrap_time`: в серии без ограничения на нетто он один допускался в
51.9% попыток, тогда как остальные три генератора — в 0.6%–1.9%. На причинных
генераторах разница между сериями оказалась в 1–2 процентных пункта на
~150 попытках каждый — то есть статистически неразличима, а не «планка ловит
бету» в том виде, как было заявлено.

**Исправление:** `bootstrap_time` теперь сэмплирует `j` только из прошлого
или настоящего (`j <= t`); строка 0 не ресэмплируется вовсе (истории до неё
нет). Добавлен регрессионный тест на причинность
(`qlab.calibration.test_noise.test_generator_does_not_use_future_reference_rows`),
общий для всех генераторов, а не только для `bootstrap_time` — он ловит
именно этот класс дефекта у ЛЮБОГО будущего генератора: возмущает эталонные
строки СТРОГО ПОСЛЕ отметки времени и проверяет, что вывод генератора до и
включительно этой отметки не изменился.

Числа ниже — повторный прогон после исправления.

## Итог

| серия | N | допущено | доля допуска | лучший sharpe_net | медиана ann_return_net | Q1 | Q3 | max |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| dollar-neutral (Σ весов = 0) | 200 | 2 | 1.0% | 0.929 | -6.38% | -9.09% | -3.12% | +4.60% |
| без ограничения на нетто (бета + carry) | 200 | 12 | 6.0% | 1.289 | -5.00% | -7.39% | -1.37% | +22.33% |

- **dollar-neutral (Σ весов = 0)**: из 200 прогонов 2 допущено (`paper`/`shelf`), 198 отклонено, 0 «нужно больше данных», 0 упали с ошибкой (структурное несоответствие или сбой стратегии — см. `error` в trial).
- **без ограничения на нетто (бета + carry)**: из 200 прогонов 12 допущено (`paper`/`shelf`), 188 отклонено, 0 «нужно больше данных», 0 упали с ошибкой (структурное несоответствие или сбой стратегии — см. `error` в trial).

## Главный результат: trend против лучшего чистого шума

Ради этого числа калибровка и делается: цифра «Шарп X» из бэктеста ничего не доказывает сама по себе, пока не известно, чего перебором достигает стратегия без всякого сигнала. Ниже — эталонная стратегия `trend-tsmom-crypto` против лучшего результата 200 книг серии «dollar-neutral (Σ весов = 0)» (чистый шум отбора инструментов, без беты и carry).

- **sharpe_net**: шум — **0.929** (лучший из 200); `trend-tsmom-crypto` — **0.305**.
- **ann_return_net**: шум — **+4.60%** (максимум из 200); `trend-tsmom-crypto` — **+4.75%**.

**trend-tsmom-crypto практически неотличим от лучшего результата чистого шума на этом окне и числе попыток** (разница по sharpe_net -0.623, по ann_return_net +0.15%). Это не значит, что trend случаен -- значит, что ни одна из этих двух цифр САМА ПО СЕБЕ, без сравнения с этим прогоном, не отличает найденный эдж от того, что даёт 200 попыток без всякого сигнала.

### Разница между сериями

Серия без ограничения на нетто допускается заметно чаще (6.0% против 1.0% у dollar-neutral, разница +5.0%). **Это значит, что планка `net_edge_positive` в текущем виде отчасти ловит рыночную бету и carry, а не отбор инструментов** — тот же класс проблемы, что и декорреляция у XSMOM: критерий, отбирающий не то. Случайная книга без нетто-ограничения в исследуемом окне не была нейтральна к рынку, и часть её доходности — это цена риска, который отбор не должен путать с эджем.

- из 2 допущенных в этой серии 2 (100%) — от одного генератора, `shuffled_instruments`; агрегатная доля допуска для этой серии в основном описывает его поведение, не «шум вообще» — см. разбивку по генераторам ниже.
- из 12 допущенных в этой серии 11 (92%) — от одного генератора, `bootstrap_time`; агрегатная доля допуска для этой серии в основном описывает его поведение, не «шум вообще» — см. разбивку по генераторам ниже.

## Разбивка по генераторам

Публикуется всегда, а не только когда что-то уже вызывает подозрение: агрегатная цифра по серии может целиком держаться на одном генераторе (конкретный прошлый случай — раздел «Ревизия» выше).

**dollar-neutral (Σ весов = 0)**

| генератор | N | допущено | доля допуска | лучший sharpe_net |
|---|---:|---:|---:|---:|
| `bootstrap_time` | 50 | 0 | 0.0% | 0.137 |
| `random_signs` | 50 | 0 | 0.0% | -0.864 |
| `random_weights` | 50 | 0 | 0.0% | -0.634 |
| `shuffled_instruments` | 50 | 2 | 4.0% | 0.929 |

**без ограничения на нетто (бета + carry)**

| генератор | N | допущено | доля допуска | лучший sharpe_net |
|---|---:|---:|---:|---:|
| `bootstrap_time` | 50 | 11 | 22.0% | 1.289 |
| `random_signs` | 50 | 0 | 0.0% | 0.013 |
| `random_weights` | 50 | 0 | 0.0% | 0.077 |
| `shuffled_instruments` | 50 | 1 | 2.0% | 0.476 |

## Три реальные стратегии — для сравнения

| стратегия | вердикт | ann_return_net | sharpe_net | min_capital_usd |
|---|---|---:|---:|---:|
| TSMOM-ансамбль на крипто-перпах HL (полный дискаверед-универс) | shelf (passed but needs $37,829 minimum capital; only $1,000 is deployable now) | +4.75% | 0.305 | $37,829 |
| Strategy B v2 — спот + трендовый хедж + храповик (BTC/ETH/SOL/AVAX) | reject (rule(s) failed: net_edge_positive; fatal rule: net_edge_positive) | +0.74% | 0.121 | $229 |
| FRAB — funding harvest, spot-long/perp-short, ranked slots (9-coin snapshot universe) | reject (rule(s) failed: net_edge_positive; fatal rule: net_edge_positive) | -1.52% | -0.157 | $36 |

- `trend-tsmom-crypto` (ann_return_net=+4.75%) выше медианы шума серии «dollar-neutral (Σ весов = 0)» (медиана -6.38%, максимум +4.60% из 200 попыток).
- `trend-tsmom-crypto` (ann_return_net=+4.75%) выше медианы шума серии «без ограничения на нетто (бета + carry)» (медиана -5.00%, максимум +22.33% из 200 попыток).

## Как строился шум

Четыре генератора (`qlab.calibration.noise`), каждый детерминирован по явному сиду: `random_weights`, `shuffled_instruments`, `random_signs`, `bootstrap_time`. Каждый использует веса эталонной стратегии как источник формы (гросс-экспозиция, оборот, число позиций, минимальный размер ноги), но не как источник сигнала — связь между инструментом и решением разрушается на разный лад в каждом генераторе (см. докстринги функций).

Структурное соответствие проверяется автоматически (`qlab.calibration.noise.check_structural_match`) для каждой сгенерированной книги: отношение gross/turnover/positions кандидата к эталону должно попасть в полосу 0.2x–8.0x, и оборот не может быть нулевым или близким к нулю ни при каких обстоятельствах — иначе книга не платит костов и допуск ничего не измеряет. Полоса асимметрична (нижняя граница туже верхней): три из четырёх генераторов перерисовывают шум заново на каждой перебалансировке, что структурно даёт БОЛЬШИЙ оборот, чем у медленного трендового сигнала — это делает допуск ТРУДНЕЕ, а не легче, поэтому широкая верхняя граница безопасна; риск свободного прохода живёт только у нижней границы, и она держится узкой.

Две серии — dollar-neutral и без ограничения на нетто — построены из ОДНОГО и того же случайного черновика на пару (генератор, сид): dollar-neutral серия это тот же черновик после `neutralize()` (обнуление суммы по строке с сохранением гросс-экспозиции и минимального размера ноги). Это изолирует именно эффект ограничения на нетто, а не какое-то другое различие между сериями.

## Оговорки

- Единственная эталонная стратегия для структурного соответствия — trend (`trend-tsmom-crypto`): это единственный из трёх спеков, который проходит весь путь без блокеров слоя данных на этом снапшоте (Bv2 и FRAB требуют спотовую ногу с неполным покрытием — docs/ACCEPTANCE_M2.md, docs/TASKS.md T19). Числа для Bv2/FRAB в таблице выше — реальные вердикты сегодняшнего прогона, но шум против ИХ формы (карри со слотами, спот+хедж) не строился в этом заходе.
- «Допущено» здесь означает `paper` ИЛИ `shelf` — обе означают, что правила приняли заявку на эдж; `shelf` лишь добавляет оговорку по капиталу. Если считать допуском только `paper`, доля будет ниже — обе цифры разумны в зависимости от вопроса.
- Лучший `sharpe_net` в таблице — максимум по ВСЕМ попыткам серии, а не только по допущенным: вопрос «что достижимо перебором» не ограничен исходом отбора.
- Ничего в этом отчёте не подгонялось под ожидаемый результат: генераторы, полоса структурного соответствия и правило `net_edge_positive` не менялись по итогам прогона, чтобы шум прошёл чаще или реже задуманного.


---

## Поправка 2026-09-22: доля допуска — не свойство правил

Всё, что написано выше, измерено **для одной формы книги** — трендовой
(`specs/trend.yaml` как эталон структурного соответствия, около 150 мелких ног,
медленный сигнал). Отчёт формулирует результат так, будто «доля допуска 1%» —
характеристика версии правил `2026-09-21.1`. Это неверно, и проверка T21 это
показала.

Тот же набор правил, откалиброванный под книгу из 20 ног на 222 инструментах
(`specs/xsmom-honest-20legs.yaml`, серия dollar-neutral, trial_id 1249–1448):

| | эталон trend (≈150 ног) | эталон XSMOM-20 (20 ног) |
|---|---:|---:|
| доля допуска, dollar-neutral | **1.0%** | **26.0%** |
| лучший `sharpe_net` шума | 0.929 | 1.193 |
| лучший `ann_return_net` шума | +4.60% | +59.73% |
| медиана `ann_return_net` шума | — | −17.11% |

Причина простая: у концентрированной книги на волатильном универсе разброс
случайного результата в разы шире, поэтому фиксированная планка в 4% годовых
отсекает у неё несравнимо меньше. Планка осмысленна относительно формы книги,
к которой её прикладывают, и сама по себе не значит ничего.

**Практическое следствие:** доля ложных допусков должна храниться при вердикте
конкретного кандидата, а не при версии правил. Вердикт без своей калибровки
не интерпретируем. Заведено в `docs/TASKS.md` как T27, пункт 3.

### И про сам инструмент калибровки

Раздел «структурное соответствие» выше рассуждает так: три генератора из
четырёх перерисовывают веса на каждой перебалансировке, дают больший оборот,
значит широкая верхняя граница полосы (8.0x) безопасна, потому что делает
допуск труднее.

На книге с недельным ребалансом это рассуждение перестаёт работать по другой
причине: **150 из 200 испытаний вообще не состоялись**, упав на проверке
соответствия с отношением оборота 8.69 при полосе до 8.0. Книга XSMOM держится
неизменной между четвергами, а генераторы перерисовывают её каждый день.
Уцелел единственный генератор, `shuffled_instruments`, — здесь он и есть
уместный, поскольку сохраняет форму и тайминг книги и разрушает ровно связь
«инструмент ↔ сигнал», но выбор был сделан не нами, а полосой.

Широкая верхняя граница не опасна, но и не безобидна: она не пропускает шум,
она **не даёт его построить**. Генераторы должны наследовать расписание
ребаланса эталонной книги, иначе калибровка молча сжимается до одного
генератора у любой стратегии с редким ребалансом — T27, пункт 5.

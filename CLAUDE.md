# q-lab

Research-лаборатория поиска и отсева торговых стратегий. **Ордеров не ставит никогда** —
исполнение живёт в funding-rate-arbitrage на хосте `10.8.0.5`.

Контекст и решения: `docs/PLAN.md`. Контракт схемы реестра: `docs/REGISTRY.md`.

## Стек

Python 3.13 + uv · SQLAlchemy 2.0 **синхронный** + Alembic + SQLite · typer · pydantic ·
structlog · FastAPI. Фронт: React 18 + Vite + Tailwind + TanStack Query + Recharts.
pandas/numpy/pyarrow для стенда. Тесты — pytest, линт — ruff.

```bash
uv sync
uv run pytest
uv run ruff check src tests
uv run qlab --help
```

## Правила

- **Вердикты выносит код, а не модель.** Правила — версионируемый YAML, движок правил —
  чистая функция. Агент может предложить метрику, но не вердикт.
- **`trial` пишется всегда**, включая выброшенные прогоны: дефляция считается по
  фактическому числу испытаний.
- **У вердикта всегда есть диапазон данных.** Воскрешение с кладбища — только на
  данных, которых не было в момент отказа.
- **Код — по-английски полностью**: имена, комментарии, докстринги, названия тестов,
  сообщения коммитов, а также `rationale` и комментарии в конфигах правил. Исключение —
  прямые цитаты из русских документов: они сохраняются дословно, перевод цитаты делает
  её недостоверной.
- Документация в `docs/` и досье — по-русски: их читает владелец.

## Бюджет токенов

Владелец должен иметь возможность пользоваться своим аккаунтом. **Агенты проекта не
занимают больше 70% любого из действующих окон лимита** — ни пятичасового, ни недельного.

Перед тем как запускать субагентов пачкой, проверить расход (`get_usage` в Claude Code
или эквивалент). Если любое окно занято на 70% и выше — **не запускать**, сообщить
владельцу и дождаться сброса. Пятичасовое окно связывает сильнее недельного: именно оно
блокирует интерактивную работу.

Порядок урезания, когда бюджета не хватает на всё — дешёвое и обязательное идёт первым,
дорогое и необязательное режется первым:

1. перепроверка дозревшего кладбища и здоровья живых стратегий (дёшево, обязательно);
2. реализация и прогон кандидатов, прошедших preflight (дорого);
3. поиск нового (режется первым).

Расход пишется в `token_spend` по стадиям — не для отчётности, а ради метрики **цены
одного выжившего**, которая отвечает на главный вопрос проекта.

## Git

- После выполнения каждого таска — коммитить и пушить в GitHub.
- Не добавлять `Co-Authored-By:` в сообщения коммитов.
